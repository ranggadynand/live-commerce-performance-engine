import {DateRange,PeriodKey} from "./types";
const iso=(d:Date)=>d.toISOString().slice(0,10); const sod=(d:Date)=>new Date(d.getFullYear(),d.getMonth(),d.getDate());
export function parseDataDate(value?:string){if(!value)return null;const s=String(value).trim();const serial=Number(s);if(Number.isFinite(serial)&&serial>20000&&serial<80000){const d=new Date((serial-25569)*86400*1000);return Number.isNaN(d.getTime())?null:d}const d=new Date(s);if(!Number.isNaN(d.getTime()))return d;const m=s.match(/^(\d{1,2})[\/-](\d{1,2})[\/-](\d{4})$/);return m?new Date(+m[3],+m[2]-1,+m[1]):null}
export const normalizeDate=(v?:string)=>{const d=parseDataDate(v);return d?iso(d):undefined};
function monday(d:Date){const x=sod(d);x.setDate(x.getDate()-((x.getDay()+6)%7));return x}
export function resolvePeriod(k:PeriodKey,cs?:string,ce?:string,now=new Date()):DateRange{const t=sod(now);let s=new Date(t),e=new Date(t);if(k==="yesterday"){s.setDate(s.getDate()-1);e=new Date(s)}if(k==="last7")s.setDate(s.getDate()-6);if(k==="last30")s.setDate(s.getDate()-29);if(k==="thisWeek")s=monday(t);if(k==="lastWeek"){e=monday(t);e.setDate(e.getDate()-1);s=new Date(e);s.setDate(s.getDate()-6)}if(k==="thisMonth")s=new Date(t.getFullYear(),t.getMonth(),1);if(k==="lastMonth"){s=new Date(t.getFullYear(),t.getMonth()-1,1);e=new Date(t.getFullYear(),t.getMonth(),0)}if(k==="custom"&&cs&&ce)return{start:cs,end:ce};return{start:iso(s),end:iso(e)}}
export function previousEquivalent(r:DateRange){const s=new Date(r.start+"T00:00:00"),e=new Date(r.end+"T00:00:00");const days=Math.round((e.getTime()-s.getTime())/86400000)+1;const pe=new Date(s);pe.setDate(pe.getDate()-1);const ps=new Date(pe);ps.setDate(ps.getDate()-(days-1));return{start:iso(ps),end:iso(pe)}}
export const inRange=(d:string|undefined,r:DateRange)=>!!d&&d>=r.start&&d<=r.end;
