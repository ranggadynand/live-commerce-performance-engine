import {google} from "googleapis";import {LiveRow,Platform} from "./types";import {normalizeDate} from "./dates";
const aliases:Record<string,string[]>={brand:["brand","account","account name"],liveId:["live id","liveid"],date:["date","tanggal","start date"],day:["day"],slot:["ls time","live time","time slot"],campaignType:["status","type","campaign type","period"],host:["host","host name","talent"],duration:["duration","live hours","hours","hour"],gmv:["gmv","revenue","live gmv"],views:["views","view","watch pv"],impressions:["live impression","live impressions","impression","impressions"],productImpressions:["product impression","product impressions"],productClicks:["product click","product clicks","clicks"],orders:["co","order","orders"],buyers:["buyers","buyer"],itemSold:["item sold","items sold"],addToCart:["add to chart","add to cart","atc"],aov:["aov"],avd:["avd","avg. viewing duration (s)","avg. view duration (s)","average viewing duration","avg viewing duration"],engagementRate:["engagement rate","er"],comments:["comment","comments"],likes:["like","likes"],follows:["follow","follows"],err:["err (%)","err","enter room rate"],ctr:["ctr (%)","ctr"],coRate:["co rate (%)","co rate","conversion rate"]};
const norm=(s:any)=>String(s??"").trim().toLowerCase().replace(/\s+/g," ");const num=(v:any)=>{if(typeof v==="number")return Number.isFinite(v)?v:0;const raw=String(v??"").trim();if(!raw)return 0;const p=raw.includes("%");const n=Number(raw.replace(/rp/gi,"").replace(/,/g,"").replace(/%/g,"").trim());return Number.isFinite(n)?(p?n/100:n):0};
function indexHeaders(h:any[]){const o:Record<string,number>={};h.forEach((x,i)=>{const n=norm(x);for(const[k,vals]of Object.entries(aliases))if(vals.includes(n)&&o[k]==null)o[k]=i});return o}
function parseRows(v:any[][],platform:Platform):LiveRow[]{if(!v.length)return[];let hi=0,b=-1;for(let i=0;i<Math.min(15,v.length);i++){const s=Object.keys(indexHeaders(v[i])).length;if(s>b){b=s;hi=i}}const idx=indexHeaders(v[hi]);return v.slice(hi+1).map(r=>({platform,brand:idx.brand!=null?String(r[idx.brand]??"").trim():"",liveId:idx.liveId!=null?String(r[idx.liveId]??"").trim():undefined,date:normalizeDate(idx.date!=null?String(r[idx.date]??""):""),day:idx.day!=null?String(r[idx.day]??"").trim():undefined,slot:idx.slot!=null?String(r[idx.slot]??"").trim():undefined,campaignType:idx.campaignType!=null?String(r[idx.campaignType]??"").trim():undefined,host:idx.host!=null?String(r[idx.host]??"").trim():undefined,duration:num(r[idx.duration]),gmv:num(r[idx.gmv]),views:num(r[idx.views]),impressions:platform==="TikTok"?num(r[idx.impressions]):undefined,productImpressions:num(r[idx.productImpressions]),productClicks:num(r[idx.productClicks]),orders:num(r[idx.orders]),buyers:num(r[idx.buyers]),itemSold:num(r[idx.itemSold]),addToCart:num(r[idx.addToCart]),aov:num(r[idx.aov]),avd:num(r[idx.avd]),engagementRate:num(r[idx.engagementRate]),comments:num(r[idx.comments]),likes:num(r[idx.likes]),follows:num(r[idx.follows]),err:platform==="TikTok"?num(r[idx.err]):undefined,ctr:num(r[idx.ctr]),coRate:num(r[idx.coRate])})).filter(r=>r.brand&&r.date&&(r.duration||r.gmv||r.views))}
async function client(){const email=process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL,key=process.env.GOOGLE_PRIVATE_KEY?.replace(/\\n/g,"\n");if(!email||!key)throw new Error("Google service account credentials are missing.");const auth=new google.auth.JWT({email,key,scopes:["https://www.googleapis.com/auth/spreadsheets.readonly"]});return google.sheets({version:"v4",auth})}
export async function loadPlatform(platform:Platform){const sheets=await client(),spreadsheetId=process.env.GOOGLE_SHEET_ID;if(!spreadsheetId)throw new Error("GOOGLE_SHEET_ID missing.");const sheetName=platform==="TikTok"?(process.env.TIKTOK_SHEET_NAME||"RAW LS TIKTOK"):(process.env.SHOPEE_SHEET_NAME||"RAW LS SHOPEE");const res=await sheets.spreadsheets.values.get({spreadsheetId,range:`'${sheetName}'!A:ZZ`,valueRenderOption:"UNFORMATTED_VALUE"});return parseRows((res.data.values||[]) as any[][],platform)}
export async function loadAll(){const[tiktok,shopee]=await Promise.all([loadPlatform("TikTok"),loadPlatform("Shopee")]);return[...tiktok,...shopee]}
